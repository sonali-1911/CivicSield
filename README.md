# VIEW APP :- https://civicshield-ai.ai.studio/

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` in .env file(create your own) to your Gemini API key
3. Run the app:
   `npm run dev`
